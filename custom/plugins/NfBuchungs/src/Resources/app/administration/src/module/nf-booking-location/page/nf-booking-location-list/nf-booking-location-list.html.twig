<sw-page class="nf-booking-location-list">

    <template #smart-bar-actions>
        <sw-button variant="primary" :router-link="{ name: 'nf.booking.location.create' }">
            {{ $t('nf-booking.location.list.buttonAdd') }}
        </sw-button>
    </template>

    <template #content>
        <sw-entity-listing
                v-if="items"
                :items="items"
                :repository="repository"
                :columns="columns"
                :show-selection="true"
                :allow-inline-edit="true"
                detail-route="nf.booking.location.detail">

            <template #column-active="{ item, isInlineEdit }">
                <template v-if="isInlineEdit">
                    <mt-checkbox v-model="item.active" />
                </template>
                <template v-else>
                    <mt-icon
                            :name="item.active ? 'regular-checkmark-xs' : 'regular-times-s'"
                            :color="item.active ? '#37d046' : '#de294c'"
                    />
                </template>
            </template>
        </sw-entity-listing>

        <sw-loader v-else></sw-loader>
    </template>
</sw-page>